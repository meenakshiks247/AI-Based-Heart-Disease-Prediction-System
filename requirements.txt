# ── Core runtime dependencies ────────────────────────────────────────
# Pin to major.minor to allow patch-level updates while avoiding
# breaking changes.  Run `pip install -r requirements.txt` to install.

# --- Web framework & server ---
fastapi>=0.104,<1.0
uvicorn>=0.24,<1.0
pydantic>=2.5,<3.0

# --- Data & ML ---
pandas>=2.1,<3.0
numpy>=1.26,<2.0
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0

# --- Boosting models (used during training & prediction) ---
xgboost>=2.0,<4.0
lightgbm>=4.0,<5.0

# --- Visualization (training scripts & notebooks) ---
matplotlib>=3.7,<4.0
