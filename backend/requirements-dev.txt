# ── Dev-only dependencies ────────────────────────────────────────────
# Install with:  pip install -r requirements-dev.txt
# This file pulls in the main requirements first, then adds dev tools.

-r ../requirements.txt

# --- Testing ---
pytest>=8.0,<9.0

# --- Linting & formatting ---
black>=24.0,<25.0
mypy>=1.8,<2.0

# --- Git hooks ---
pre-commit>=3.6,<4.0
